/* Escreva um algoritmo para ler as dimensão de um quadrado (base), calcular e escrever a 
área do quadrado.  */

let base = parseFloat(prompt("Digite a base do quadrado:"))

area = Math.pow(base,2)

console.log(`a área do seu quadrado é ${area}`)